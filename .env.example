# Copy to .env and fill in. .env is git-ignored — never commit real keys.
ANTHROPIC_API_KEY=""
# Optional: enables real YouTube video curation. Without it, lessons link to YouTube searches instead.
YOUTUBE_API_KEY=""
DATABASE_URL="file:./dev.db"
