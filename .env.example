# Copy to .env and fill in. .env is git-ignored — never commit real keys.
ANTHROPIC_API_KEY=""
# Optional: enables real YouTube video curation. Without it, lessons link to YouTube searches instead.
YOUTUBE_API_KEY=""
# Neon Postgres. On Vercel these are injected by the Neon integration;
# for local dev, copy both from the Neon console (Connect panel).
POSTGRES_PRISMA_URL=""
DATABASE_URL_UNPOOLED=""
